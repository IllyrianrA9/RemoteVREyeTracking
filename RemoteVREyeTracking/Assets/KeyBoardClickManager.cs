using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class KeyBoardClickManager : MonoBehaviour
{
    public GameObject buttonStartExperiment;
    public GameObject explanationText;
    public GameObject ballMovement;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.Space))
        //{
        //    buttonStartExperiment.gameObject.SetActive(false);
        //    explanationText.gameObject.SetActive(false);
        //    ballMovement.gameObject.SetActive(true);
        //}
        
    }
}
