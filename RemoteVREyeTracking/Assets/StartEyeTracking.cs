using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartEyeTracking : MonoBehaviour
{
    public GameObject startEyeTracking;
    // Start is called before the first frame update
    void Start()
    {
        startEyeTracking.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
